#ANS1

from datetime import datetime
now = datetime.now() 
print(now.hour) 



#1) Create an abstract class called "Animal" that has 2 abstract methods :
#a) printName()
#b) printSound()
#2) Create 3 classes "Dog", "Cat" and "Cow" that extends the Animal Class. 
#3) Create a small test program (main).

abstract class animal {
void printName();
void printSound();
}
    
#ANS. b
class dog extends animal {
    void printName() {
        print("Dog");
    }

    void printSound(){
        print("Woof");
    }
}
class cat extends animal{
    void printName(){
        print("Cat");
    }

    void printSound(){
        print("Meow");
    }
}
class caw extends animal{
    void printName(){
        print("Caw");
    }

    void printSound("Mww"){
        print("Mww");
    }
}

void main(){
    var c = cat(), d=dog(), w=cow();
    c.printName();
    c.printSound();
    d.printName();
    d.printSound();
    w.printName();
    w.printSound();
    }
  
  
  
  
#Write a program that takes an Integer (0 < x <= 10) then takes X integers one after another, after inputting all those numbers, 
#you should print 'N is odd' or 'N is even' depending on each N that was received. 
#Your whole program should be implemented using a single function.
num = int(input("Enter a number: "))
if (num % 2) == 0:
   print("{0} is Even".format(num))
else:
   print("{0} is Odd".format(num))
